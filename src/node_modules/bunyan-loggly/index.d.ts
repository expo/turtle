export = BunyanLoggly;

declare class BunyanLoggly {
  constructor(options: BunyanLoggly.IOptions, bufferLength?: number, bufferTimeout?: number, logglyCallback?: any);
  public write(data: any): void;
}

declare namespace BunyanLoggly {
  interface IOptions {
    token: string;
    subdomain: string;
  }
}
